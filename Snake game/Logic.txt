Tạo 2 lớp:

1. Game init: 
-Khung boardgame.
-Rắn chạm đầu vào khung thì endgame.
-Vẽ rắn




2. Lớp Snake:
-Tốc độ: Không đổi.
-Chiều rộng rắn: Không đổi.
-Chiều dài: Có đổi sau khi ăn Food.

+Di chuyển + dâm đầu vào đuôi.
snake.x = snake.x + snake.dx;
snake.y = snake.y+ snake.dy; // Moi 1 loop ran di chuyen them 1 dvi dx, dy

+Ăn Food + tạo đuôi


3. Lớp Food:
-Chiều dài
-Chiều rộng

+Di chuyển: random
+Xuất hiện: Mất đi sau khi bị ăn và xuất hiện lại trên board.



  // Phương thức unshift sẽ thêm một hoặc nhiều phần tử vào đầu mảng

  snake.cells.shift({x: snake.x, y: snake.y});

  // thêm 1 ô vuông phía trc thì phải remove 1 cái phía sau để snake move dc.

  if (snake.cells.length > snake.maxCells) {

    snake.cells.pop();
